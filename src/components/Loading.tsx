import * as React from "react"

const Loader = ({ ...props }) => {
  return (
    <main className="w-full h-screen bg-black fixed top-0 text-white flex items-center jusify-center">
      Loading ...
    </main>
  )
}

export default Loader
